{% extends "pages/groundhogs.njk" %}

{% block body_classes -%}
  groundhogs--canada
{%- endblock %}

{% block footer_scripts %}
  <script src="/javascripts/vendor/list.min.js"></script>
  <script>
    new List('sortable--groundhogs', {
      valueNames: ['name', 'type', 'country', 'latest', 'count']
    });
  </script>
{% endblock %}

{% block groundhogsHeader %}
  {{ page_header({ h1: title, backUrl: '/groundhogs', backText: 'All groundhogs', dataUrl: '/api/v1/groundhogs?isGroundhog=false' }) }}
{% endblock %}

{% block groundhogsIntro %}
  <p>There are <strong>{{ groundhogs | length }}</strong> non-traditional prognosticators who made predictions in 2022.</p>
  <p>It turns out that ‘Groundhog’ is more like a job title and less about the species of animal. Alternative prognosticators can include other animals, taxidermied groundhogs, plush stuffed animals, or folks in costume (think sports mascots).</p>
    <p>If you don’t see your favourite atypical prognosticator, feel free to
      <a class="link--icon" href="add-groundhog">
        {{ icon({ add: true }) }} <span>add a groundhog</span>, eh
      </a>.
    </p>
{% endblock %}}

{% block groundhogsPlaceholder %}Lucy…{% endblock %}

{% block groundhogsTable %}
  <div class="groundhogs data-table" role="region" aria-labelledby="data-table--groundhogs" tabindex="0">
    <table class="data-table__table">
      <caption id="data-table--groundhogs">
        <h2 class="data-table__title h4">Alternative groundhogs</h2>
      </caption>
      <thead class="visible">
        <tr>
          <th class="sort" data-sort="name">Name</th>
          <th class="sort" data-sort="type">Type</th>
          <th class="sort" data-sort="country">Country</th>
          <th class="sort extra-padding" data-sort="latest">Latest prediction</th>
          <th class="sort extra-padding" data-sort="count" data-default-order="desc">Total predictions</th>
        </tr>
      </thead>
      <tbody class="list">
        {% for groundhog in groundhogs %}
          <tr onclick="window.location='/groundhogs/{{ groundhog.slug }}';">
            <th class="name"><a href="/groundhogs/{{ groundhog.slug }}" class="underline">{{ groundhog.name }}</a></th>
            <td class="type">
              {{ groundhog.type }}
            </td>
            <td class="country">
              {{ groundhog.country }}
            </td>
            <td class="latest">
              {% if groundhog.latestPrediction.shadow %}
                <span role="img" aria-label="Longer winter">❄️</span>
              {% else %}
                <span role="img" aria-label="Early spring">🌼</span>
              {% endif %}
            </td>
            <td class="count">
              {{ groundhog.predictionsCount }}
            </td>
          </tr>
        {% endfor %}
      </tbody>
    </table>
  </div>
{% endblock %}
